# MiMo 模型余量检测 · 使用说明

## 装之前先确认五件事

**1. DesireCore 客户端是 10.0.156 或更高版本。** 本 Agent 依赖这个版本里的几项平台修复：定时任务的完成判据、内置浏览器长时间运行的稳定性、暂停后恢复的调度不丢工具。

**2. 账号库里有你的 MiMo 账号。** 在 DesireCore 的「资源 → 算力 → 账号」里新增一条账号，「站点 / 服务」填 `platform.xiaomimimo.com`（Agent 靠它认出这是 MiMo 账号，不填会提示账号库里没有），记下这条账号的 id（例如 `mimo-main`）。巡检时就用这个 id 指定账号。密码由 DesireCore 加密保存，Agent 只用占位符填登录表单，看不到明文。

**3. 第一次巡检请在对话里做，并由你完成一次短信验证。** 对 Agent 说「用 mimo-main 查一次 MiMo 余量」。它会为这个账号新建一个持久化浏览器环境，打开登录页、代填账号密码，停在短信验证页并告诉你。你在浏览器面板里点「发送验证码」、输入验证码，完成后点面板上的「恢复 Agent」，**再回复它一句**，它就会接着取数。验证完一定要回复：它会一直占着这个浏览器环境等你，期间的定时巡检会记为「被占用」。登录态保存在这个环境里，之后一般不用再登；Cookie 过期时它会先尝试免填表恢复，恢复不了才提醒你。

**4. 一个账号对应一个浏览器环境。** 多个账号各自独立，对应关系记在工作目录下的 `mimo-quota/accounts.json`，在对话里首次巡检时自动创建。请不要让两个账号共用一个浏览器环境——小米的登录 Cookie 会互相覆盖。

**5. 它只读，也不执行命令。** 只读取套餐详情、用量、余额和登录状态四个查询接口，不会购买、续费、退订、改套餐、重置 API Key 或退出登录。整个巡检只用内置浏览器和文件工具，不执行任何命令；如果在运行记录里看到它执行命令，说明模型跑偏了。

## 怎么用

- 「查一下 MiMo 余量」「用 mimo-main 查一次」——立即巡检一次并汇报
- 「每小时巡检一次 mimo-main」——让它帮你建定时调度
- 「上次巡检结果是什么」——读最近一次结果，不重新打开浏览器

## 定时巡检怎么配

先按上面第 3 条在对话里跑通一次，再对它说「每小时巡检一次 mimo-main」。它会给这个账号建一个调度，并设置「完成判据」，要求每轮写出 `mimo-quota/mimo-main/latest.json`。完成判据目前只能通过 Agent 设置，所以请让它来建，不要自己在界面上手工建。

首次巡检和建调度都请在这个 Agent 自己的对话里做，不要放在团队或单独指定了工作目录的话题里：定时巡检用的是 Agent 的默认工作目录，工作目录不同就找不到浏览器环境的对应关系。

设了完成判据，模型没做完就收场时，平台会接着原会话要求它做完；仍没做完就记为失败并通知你，不会悄悄漏掉一轮。

定时巡检不会新建浏览器环境，也不会在上一轮已经需要人工登录时反复填表（反复填表容易触发小米风控）。登录失效又恢复不了时，它会提醒你；到对话里让它再查一次，按第 3 条完成验证即可。

关于审批：
- 读取接口用的是浏览器导航与读取页面文本，不需要审批。
- 新建浏览器环境、写结果文件会发起审批。默认的 AI 审批模式下，对话里会先留一小段时间（默认约 30 秒）给你亲自决定，超时由 AI 审批代为判断；定时运行里直接由 AI 审批判断。

关于模型：默认使用智能路由的旗舰档。多步浏览器操作对模型要求较高，实测轻量模型容易半途收场、忘记上下文或做与巡检无关的事，不建议降档。

## 你会拿到什么

工作目录下多出一个 `mimo-quota/` 目录：

```
mimo-quota/
├── accounts.json            账号与浏览器环境的对应关系
└── mimo-main/
    ├── latest.json          最近一次结果
    └── history/             历次结果，每轮一个文件
```

每条结果包含套餐档位、总额度、已用、剩余、剩余百分比、重置时间（UTC 原文和北京时间都给）、与上次成功巡检相比的变化、数据来源（接口还是页面）、数据质量，以及本轮成立的告警项。

## 什么时候会提醒你

每轮定时巡检结束后，这个 Agent 的对话里都会出现一条一行的执行回执。除此之外，出现下面这些情况时它会主动发消息提醒你。**同一情况持续期间只提醒一次**：比如余量一直在 15%，只在第一次跌破 20% 时提醒，中间偶尔有一轮没查成也不会重复提醒；额度重置回满后也不会误报。

- 剩余额度降到 20% 及以下
- 距离额度重置不超过 3 天
- 相邻两次成功巡检之间，剩余额度下降 10 个百分点以上（两次间隔不超过 24 小时；消耗异常）
- 数据校验对不上
- 需要你处理的问题：账号库里找不到账号、账号还没有浏览器环境、登录失效且自动恢复失败、遇到人机验证、页面改版等
- 可能自行恢复的问题（浏览器暂时不可用、网络失败、浏览器环境被占用、会话中断、操作结果未知）连续两轮都没恢复

## 它不会做的事

- 不花钱，不改套餐和账户设置，不访问查询白名单以外的接口
- 不绕过滑块、图形验证码或短信验证，遇到就停下请你处理
- 不保存、不输出密码、Cookie、验证码或完整 API Key；手机号只保留后 4 位
- 不编造数字：取不到就记为「未取到」并说明卡在哪一步
- 不会因为一个账号登录不上就换用另一个账号
- 不执行命令、不启动其他程序

## 已知限制

- 新浏览器环境第一次登录必须由你完成短信验证
- 定时巡检需要 DesireCore 保持运行；电脑休眠或客户端退出期间不会巡检
- 页面或接口改版时会记为「页面改版」并提醒你，不会猜测数值
- 同一个账号的两轮巡检如果时间重叠（比如手动试跑撞上定时轮次），后一轮会记为被占用，一般下一轮就恢复；连续两轮被占用才会提醒你
- 网络长时间中断，或内置浏览器打不开页面时，这一轮会记为「浏览器暂时不可用」，连续两轮都没恢复才会提醒你。断网持续较久时，写结果文件所需的 AI 审批也可能失败，这一轮会被记为失败

## 隐私

账号信息只在你本机的 DesireCore 里流转：密码在加密存储中，登录态在本机的浏览器环境里，巡检结果只写在你的工作目录。本 Agent 不会把这些信息发往 MiMo 开放平台以外的任何地方。
