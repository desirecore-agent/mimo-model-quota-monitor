# 更新日志

## 1.0.0 — 2026-09-12

- 首次发布到市场：MiMo 模型余量检测，巡检小米 MiMo 开放平台 Token Plan 的套餐额度、用量、余额与重置时间
- 需要 DesireCore 10.0.156 或更高版本（依赖调度完成判据、内置浏览器长时间运行的稳定性修复、调度暂停恢复不丢工具）
- 内置私有技能 `mimo-quota-check`（巡检 SOP 与建定时巡检的做法），`references/` 附结果与账号映射两份 JSON Schema
- 全程不执行任何命令：只用内置浏览器、`Read`、`Write` 与 `MathCalc`，Windows 上不依赖 Git Bash；时刻差与时区换算用 SOP 里给定的 `MathCalc` 表达式
- 多账号：一个账号独占一个持久化浏览器环境，对应关系记在工作目录 `mimo-quota/accounts.json`；浏览器环境只在对话里创建，定时运行不建
- 登录：401 时先导航 `loginUrl` 免填表恢复，失败再用账号库占位符填表；点击后留在当前页面判定人机验证或登录失败；遇到滑块、图形码、短信验证立即停下。首次登录在对话里完成，Agent 保留会话等你验证并回复后继续；定时运行里上一轮已需要人工登录时不再重复填表
- 落盘：每轮一份 `history/<采集时刻>.json`，最后一步覆盖写 `latest.json`（定时巡检的完成判据）；最近一次成功巡检的数字随结果逐轮带下去，不需要翻历史
- 提醒：每轮记录成立的告警项（余量 ≤ 20%、距重置 ≤ 3 天、相邻两次成功巡检之间下降 ≥ 10 个百分点、数据存疑、各类阻塞），只在出现上一轮与最近一次成功巡检都没有的告警项时提醒；额度重置回满不算异常；可能自行恢复的阻塞连续两轮才提醒
- 无人值守纪律：定时运行里不提问、不重做已完成的步骤、不做与巡检无关的事、最后回复只写一行摘要；浏览器平台报错只记为阻塞，不新建浏览器环境、不改映射表
- 站点经验只记站点事实：定时运行里不写站点经验，避免被降级写到用户层后遮蔽整份站点经验
- 模型默认使用智能路由旗舰档；心跳关闭，`HEARTBEAT.md` 只留标题，心跳开着也不会白调模型
